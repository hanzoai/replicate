name: Upgrade Tests

on:
  pull_request:
    paths:
      - '**.go'
      - 'go.mod'
      - 'go.sum'
      - 'tests/integration/**'
      - '.github/workflows/upgrade-tests.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  upgrade-test:
    name: v0.3.x to v0.5.x Upgrade Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: "go.mod"

      - name: Download Replicate v0.3.13
        run: |
          mkdir -p /tmp/replicate-v3
          gh release download v0.3.13 --repo benbjohnson/replicate --pattern 'replicate-v0.3.13-linux-amd64.tar.gz' --dir /tmp
          tar -xzf /tmp/replicate-v0.3.13-linux-amd64.tar.gz -C /tmp/replicate-v3
          chmod +x /tmp/replicate-v3/replicate
          /tmp/replicate-v3/replicate version
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Build current binaries
        run: go build -o bin/replicate ./cmd/replicate && go build -o bin/replicate-test ./cmd/replicate-test

      - name: Run upgrade integration tests
        run: go test -v -tags=integration -timeout=10m ./tests/integration/... -run=TestUpgrade
        env:
          CGO_ENABLED: "1"
          REPLICATE_V3_BIN: /tmp/replicate-v3/replicate

      - name: Upload test logs
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: upgrade-test-logs
          path: /tmp/replicate-*/*.log
