# replicate-vfs-darwin-arm64

Platform package for `replicate-vfs` — macOS ARM64 (Apple Silicon).

This package is installed automatically by `replicate-vfs`. You should not need to install it directly.
